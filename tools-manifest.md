# Tools Manifest — MCP Factory Connectivity Layer

> Generated by the `factory/mcp-setup` bootstrap. This document is the single source of truth for every MCP server and external tool wired into the project. Keep it in sync with @mcp.config.json.

## Overview

Eight MCP servers across three tiers give autonomous agents full end-to-end capability:

| Tier | Purpose | Servers |
|------|---------|---------|
| **Core** | Baseline agent operation | github, playwright, filesystem |
| **Deployment** | CI/CD and hosted service control | vercel, railway |
| **Knowledge** | Persistent memory and external data | notion, fetch, memory |

All env vars live in @configs/.env.example. Real secrets go into `.env` (gitignored).

---

## Tier 1 — Core

### 1. GitHub MCP
- **Purpose**: Repository management — issues, pull requests, checks, releases, branches, commits, code search.
- **Package**: `@modelcontextprotocol/server-github`
- **Transport**: stdio via `npx`
- **Env vars**: `GITHUB_PERSONAL_ACCESS_TOKEN` (scopes: `repo`, `workflow`, `read:org`, `read:packages`)
- **Verify**: list open PRs and issues on `muffy86/autogen-api-style-agent`.
- **Used by**: repo-bootstrap, bug-triage, ci-repair, pr-hardening, release-ship skills.

### 2. Playwright MCP
- **Purpose**: Browser automation and UI verification — navigate pages, click, type, take screenshots, evaluate JS.
- **Package**: `@playwright/mcp`
- **Transport**: stdio via `npx`
- **Env vars**: none (browsers installed on first use via `npx playwright install`)
- **Verify**: open `https://github.com/muffy86/autogen-api-style-agent` and return the page title.
- **Used by**: post-deploy-verify, pr-hardening skills.

### 3. Filesystem MCP
- **Purpose**: Workspace file I/O — read, write, list, move, search within the project root. Scoped to `${workspaceFolder}` — cannot escape.
- **Package**: `@modelcontextprotocol/server-filesystem`
- **Transport**: stdio via `npx`
- **Env vars**: none (path passed as arg)
- **Verify**: list contents of `src/`.
- **Used by**: every skill.

---

## Tier 2 — Deployment

### 4. Vercel MCP
- **Purpose**: Vercel deployments — trigger builds, read preview URLs, manage environment variables, monitor deploy status, inspect logs.
- **Endpoint**: `https://mcp.vercel.com` (remote HTTP MCP)
- **Transport**: HTTP
- **Env vars**: `VERCEL_TOKEN`, `VERCEL_TEAM_ID`, `VERCEL_PROJECT_ID`
- **Verify**: list deployments for the project.
- **Used by**: release-ship, post-deploy-verify skills.

### 5. Railway MCP
- **Purpose**: Railway services — deploy hooks, log streaming, env vars, service lifecycle, database provisioning.
- **Package**: `@jasontanswe/railway-mcp`
- **Transport**: stdio via `npx`
- **Env vars**: `RAILWAY_API_TOKEN`
- **Verify**: list connected services.
- **Used by**: release-ship, post-deploy-verify skills.

---

## Tier 3 — Knowledge + Orchestration

### 6. Notion MCP
- **Purpose**: Internal runbooks, specs, and project documentation. Read and write Notion pages and databases.
- **Package**: `@notionhq/notion-mcp-server`
- **Transport**: stdio via `npx`
- **Env vars**: `NOTION_API_KEY` (injected into `OPENAPI_MCP_HEADERS` as Bearer token)
- **Verify**: search for a page titled "Runbook".
- **Used by**: repo-bootstrap, release-ship, bug-triage skills.

### 7. Fetch MCP
- **Purpose**: Generic HTTP — hit API endpoints, webhook testing, health checks, page content extraction.
- **Package**: `mcp-server-fetch` (Python, runs via `uvx`)
- **Transport**: stdio via `uvx`
- **Env vars**: none
- **Verify**: fetch `https://api.github.com/zen`.
- **Used by**: ci-repair, post-deploy-verify skills.

### 8. Memory MCP
- **Purpose**: Persistent knowledge graph — stores agent context, decisions, and outputs across sessions. Survives restarts.
- **Package**: `@modelcontextprotocol/server-memory`
- **Transport**: stdio via `npx`
- **Env vars**: `MEMORY_FILE_PATH` (defaults to `.capy/memory/knowledge-graph.json`)
- **Verify**: store and retrieve a test entity.
- **Used by**: every skill (long-running context).

---

## Activation checklist

- [ ] Copy `configs/.env.example` → `.env` and populate secrets
- [ ] Ensure Node 18+ and Python 3.10+ are installed (for `npx` and `uvx` respectively)
- [ ] Run `uv tool install mcp-server-fetch` or rely on `uvx` caching on first call
- [ ] Run `npx playwright install` once to cache browsers
- [ ] Point your MCP client (Claude Desktop, Trae, Cursor, etc.) at @mcp.config.json
- [ ] Verify each server responds to its tools-list probe

## GitHub MCP activation check (performed during setup)

- Repository: `muffy86/autogen-api-style-agent`
- Open PRs: 6 (see `gh pr list`)
- Open Issues: 0
- `gh` CLI confirms connectivity; MCP server uses the same token and scopes.

## Security posture

- No secrets are committed — only variable names.
- `.env` is gitignored (verify in @.gitignore).
- Filesystem MCP is scoped to `${workspaceFolder}` — cannot read outside the project.
- Tokens should use minimum scopes required and be rotated quarterly.
